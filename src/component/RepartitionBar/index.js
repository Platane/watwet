import { h, Component } from 'preact'
import styled from 'preact-emotion'

const toSegment = values => {}

export const RepartitionBar = ({ values, labels, colors, onChange }) => (
  <Container />
)

const Container = styled.div``
